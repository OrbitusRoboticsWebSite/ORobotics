import * as THREE from 'three';
import {
  MAX_ROB_HEALTH,
  applyROBHealthDamage,
  bossStats,
  finishes,
  firstProjectileImpact,
  isUnlocked,
  meleeAnimationIsClear,
  meleeWeapons,
  rangedWeapons,
  unlockReward,
  weaponDamage,
} from './rob-game-rules.mjs';

const root = document.querySelector('[data-rob-simulator]');
if (root) {
  const canvas = root.querySelector('[data-sim-canvas]');
  const viewport = root.querySelector('[data-sim-viewport]');
  const shell = root.querySelector('[data-sim-shell]');
  const renderer = new THREE.WebGLRenderer({ canvas, antialias: true });
  renderer.setPixelRatio(Math.min(devicePixelRatio, 1.75)); renderer.shadowMap.enabled = true;
  const scene = new THREE.Scene(); scene.background = new THREE.Color(0x07111b); scene.fog = new THREE.Fog(0x07111b, 28, 52);
  const camera = new THREE.PerspectiveCamera(55, 1, .1, 90); const clock = new THREE.Clock();
  const controls = { left: 0, right: 0 }, touch = { forward: 0, steering: 0, left: 0, right: 0, leftActive: false, rightActive: false }; const keys = new Set(), activeDrivePointers = new Map(), activeTreadPointers = new Map();
  const obstacles = [], cells = [], bolts = [], enemyBolts = [], enemies = [], levelParts = [];
  const ARENA_HALF_WIDTH = 16, ARENA_HALF_DEPTH = 12, LEVEL_SCALE = 1.38, ROBOT_HALF_WIDTH = 1.12, ROBOT_HALF_LENGTH = 1.13, ARENA_CLEARANCE = .18;
  const readProgress = (key, fallback) => { try { return localStorage.getItem(key) ?? fallback; } catch { return fallback; } };
  const saveProgress = (key, value) => { try { localStorage.setItem(key, String(value)); } catch {} };
  let running = false, complete = false, levelComplete = false, elapsed = 0, levelElapsed = 0, score = 0, gateDone = false, cellCount = 0, lastShot = -Infinity, levelIndex = 0, hasKey = false, doorOpen = true, laserLock, laserChargeStarted, saberCombo = 0, lastSaberAttack = -Infinity, saberAnimation, gamepadLaserHeld = false;
  let health = MAX_ROB_HEALTH, damageInvulnerableUntil = -Infinity, highestCompletedLevel = Math.max(0, Math.min(15, Number(readProgress('robHighestCompletedLevel', 0)) || 0));
  let selectedFinishID = readProgress('robRobotFinish', 'graphite'), selectedRangedID = readProgress('robRangedWeapon', 'shoulderGatling'), selectedMeleeID = readProgress('robMeleeWeapon', 'dualSabers');
  const selectedFinish = () => finishes.find(({ id }) => id === selectedFinishID) || finishes[0];
  const selectedRanged = () => rangedWeapons.find((weapon) => weapon.id === selectedRangedID && isUnlocked(weapon, highestCompletedLevel)) || rangedWeapons[0];
  const selectedMelee = () => meleeWeapons.find((weapon) => weapon.id === selectedMeleeID && isUnlocked(weapon, highestCompletedLevel)) || meleeWeapons[0];
  const levels = [
    { name: 'Calibration Deck', floor: 0x172734, grid: 0x2ca4bb, gate: [0, -3.6], dock: [7.8, 5.7], cells: [[-7.5, -4.8], [.8, 3.3], [7.5, -.8]], enemies: [['spider', -7.2, .2], ['dalek', 6.8, -5.4], ['spider', 0, -4.8]], health: 2, speed: 0, bonus: 900, obstacles: [[-3.2, -.8, 2.7, 1.3, 1.1], [3.8, 1.6, 1.5, 3.2, 1.35], [-1.3, 4.7, 3.1, 1.2, .85], [6.6, -3.6, 1.2, 2.1, 1.6]] },
    { name: 'Key Workshop', floor: 0x241b32, grid: 0xc548e8, gate: [-7.8, 1.7], dock: [7.7, -5.7], key: [-7.7, -5.5], door: [2.8, 0, .5, 5], cells: [[-1.3, -2.3], [7.2, 1.5], [4.5, -4.8]], enemies: [['spider', -4.8, 3.8], ['dalek', 6, -4], ['spider', -1, -4.6]], health: 2, speed: .18, bonus: 1100, obstacles: [[-5.5, -.5, 1.1, 5.8, 1.5], [-.4, 2.1, 5.2, 1, 1.1]] },
    { name: 'Crossroads', floor: 0x1c2924, grid: 0x55dd88, gate: [7.5, 3.8], dock: [-7.8, -5.8], key: [8, 5], door: [-2, 1.5, 5, .5], cells: [[-8, 4.7], [-3.7, -.8], [0, -5.8], [7, -3]], enemies: [['spider', -6.8, 1.1], ['dalek', -1.5, -4.6], ['spider', 6, 3.8]], health: 2, speed: .24, bonus: 1300, obstacles: [[-6.2, -2.4, 5.5, .9, 1.2], [0, -.8, 4.8, .9, 1.1], [6.2, .1, 4.8, .9, 1.3]] },
    { name: 'Sensor Hall', floor: 0x152b35, grid: 0x38dfff, gate: [-7, -4], dock: [8, 5.5], cells: [[-7, 5], [-1, -4], [4, 4], [7, -4]], enemies: [['spider', -4, 1], ['dalek', 5, -2], ['spider', -7, -4], ['dalek', 7, 4]], health: 2, speed: .25, bonus: 1500, obstacles: [[-4, -2, 1, 6, 1], [0, 2, 1, 6, 1], [4, -2, 1, 6, 1]] },
    { name: 'Amber Armory', floor: 0x30251a, grid: 0xf1b93a, gate: [0, -5], dock: [8, -5], key: [-8, 5], door: [0, 1, 4, .5], cells: [[-6, 4], [2, -4], [7, -2], [5, 4]], enemies: [['dalek', -3, 3], ['spider', 5, -3], ['dalek', 7, 4], ['spider', -7, -4]], health: 3, speed: .3, bonus: 1700, obstacles: [[-4, 0, 1, 5, 1], [4, -2, 1, 3, 1]] },
    { name: 'Switchback Foundry', floor: 0x2b1830, grid: 0xd65cff, gate: [-8, 0], dock: [8, -5], key: [0, 5], door: [5, -2, .5, 5], cells: [[-7, -5], [-3, 3], [3, -4], [7, 4], [0, 0]], enemies: [['spider', -4, 0], ['dalek', 2, 2], ['spider', 6, -4], ['dalek', -7, 4], ['spider', 7, 4]], health: 3, speed: .35, bonus: 1900, obstacles: [[-5, -2, 5, .7, 1], [-1, 2, 5, .7, 1], [3, -2, 5, .7, 1]] },
    { name: 'Twin Sentinel Bay', floor: 0x17282a, grid: 0x55dd88, gate: [0, -5], dock: [0, 5.5], cells: [[-7, -4], [7, -4], [-7, 4], [7, 4], [0, 0]], enemies: [['dalek', -5, 0], ['dalek', 5, 0], ['spider', 0, -3], ['spider', -7, 4], ['dalek', 7, 4]], health: 3, speed: .4, bonus: 2100, obstacles: [[-2.5, 0, .7, 5, 1], [2.5, 0, .7, 5, 1]] },
    { name: 'Power Relay', floor: 0x2e2614, grid: 0xffc83d, gate: [-8, 5], dock: [8, -5], key: [-8, -5], door: [1, 0, .5, 5], cells: [[-5, 2], [2, -4], [5, 0], [7, -4], [0, 4]], enemies: [['spider', -3, -2], ['dalek', 3, 3], ['spider', 7, 0], ['dalek', -7, 4], ['spider', 7, -4]], health: 4, speed: .42, bonus: 2300, obstacles: [[-4, 0, .8, 5, 1], [4, 2.5, .8, 2, 1]] },
    { name: 'Guardian Maze', floor: 0x1a2430, grid: 0x5aa8ff, gate: [8, -5], dock: [-8, -5], key: [8, 5], door: [-5, 1, 4, .5], cells: [[-8, -5], [-3, -3], [0, 4], [4, -2], [7, 2], [-7, 4]], enemies: [['spider', -6, 0], ['dalek', -1, -4], ['spider', 3, 3], ['dalek', 7, -3], ['spider', -7, 4], ['dalek', 6, 4]], health: 4, speed: .46, bonus: 2600, obstacles: [[-5, -2, 5, .7, 1], [-1, -2, .7, 4, 1], [3, -2, 5, .7, 1], [6, 3.5, .7, 2, 1]] },
    { name: 'Mission Control', floor: 0x132a25, grid: 0x2bdf8a, gate: [0, -5], dock: [0, -5.5], key: [-8, 5], door: [0, 2, 5, .5], cells: [[-8, 5], [-5, -2], [0, -3], [5, -2], [8, -5], [7, 4]], enemies: [['dalek', -6, -2], ['spider', -2, 3], ['dalek', 3, -2], ['spider', 7, 3], ['dalek', -7, 4], ['spider', 6, -5]], health: 4, speed: .5, bonus: 3000, obstacles: [[-5, -2, .8, 4, 1], [0, -1, .8, 4, 1], [5, -2, .8, 4, 1]] },
    { name: 'Reactor Run', floor: 0x25151a, grid: 0xff5c72, gate: [-8, -5], dock: [8, 5], cells: [[-8, 5], [-5, -4], [-1, 3], [3, -4], [7, 0], [8, 5]], enemies: [['spider', -7, 0], ['spider', -3, 4], ['dalek', 1, -4], ['spider', 5, 3], ['dalek', 7, -3], ['dalek', 0, 1]], health: 4, speed: .54, bonus: 3300, obstacles: [[-6, -2, 4, .8, 1.2], [-1, 2, 5, .8, 1.2], [5, -2, 4, .8, 1.2]] },
    { name: 'Eclipse Hangar', floor: 0x17152d, grid: 0xa876ff, gate: [8, 5], dock: [-8, -5], key: [-8, 5], door: [2, 0, .5, 5], cells: [[-7, -4], [-5, 3], [-1, -3], [3, 4], [6, -4], [8, 2]], enemies: [['dalek', -7, 0], ['spider', -4, -4], ['dalek', -1, 4], ['spider', 3, -3], ['dalek', 6, 4], ['spider', 8, -2]], health: 4, speed: .58, bonus: 3600, obstacles: [[-5, 0, 1, 6, 1.2], [5, 2, 1, 4, 1.2]] },
    { name: 'Quantum Causeway', floor: 0x10293a, grid: 0x38dfff, gate: [0, -5], dock: [8, 5], key: [-8, 5], door: [-2, 1, 5, .5], cells: [[-8, -4], [-6, 4], [-3, -1], [0, 4], [3, -4], [6, 1], [8, 5]], enemies: [['spider', -8, 0], ['dalek', -5, -4], ['spider', -2, 4], ['dalek', 1, -3], ['spider', 4, 4], ['dalek', 7, -2], ['spider', 8, 4]], health: 5, speed: .62, bonus: 3900, obstacles: [[-6, -2, 4, .7, 1], [-1, 2, 4, .7, 1], [4, -2, 4, .7, 1]] },
    { name: 'Siege Foundry', floor: 0x301b12, grid: 0xff9b45, gate: [-8, 0], dock: [8, -5], key: [0, 5], door: [4, -1, .5, 5], cells: [[-8, -5], [-6, 3], [-3, -3], [0, 4], [3, -4], [6, 3], [8, -2]], enemies: [['dalek', -8, 1], ['dalek', -5, -4], ['spider', -2, 3], ['dalek', 1, -3], ['spider', 4, 4], ['spider', 7, -4], ['dalek', 8, 2]], health: 5, speed: .66, bonus: 4200, obstacles: [[-6, 0, 1, 6, 1.4], [-1, -2, 1, 4, 1.2], [4, 2, 1, 4, 1.2]] },
    { name: 'Final Citadel', floor: 0x10261d, grid: 0x2bdf8a, gate: [0, -5], dock: [0, 5.5], key: [-8, 5], door: [0, 1, 5, .5], cells: [[-8, -5], [-8, 5], [-5, 0], [-2, -4], [2, 4], [5, 0], [8, -5], [8, 5]], enemies: [['spider', -8, 0], ['dalek', -6, -4], ['spider', -4, 4], ['dalek', -1, -3], ['spider', 2, 4], ['dalek', 5, -4], ['spider', 8, 1], ['dalek', 7, 5]], health: 5, speed: .7, bonus: 4800, obstacles: [[-6, -2, 4, .8, 1.3], [-1, 2, 4, .8, 1.3], [4, -2, 4, .8, 1.3], [7, 3, 1, 3, 1.3]] },
  ].map((level) => ({ ...level, gate: level.gate.map((value) => value * LEVEL_SCALE), dock: level.dock.map((value) => value * LEVEL_SCALE), key: level.key?.map((value) => value * LEVEL_SCALE), door: level.door?.map((value, index) => index < 4 ? value * LEVEL_SCALE : value), cells: level.cells.map((point) => point.map((value) => value * LEVEL_SCALE)), enemies: level.enemies.map(([type, x, z]) => [type, x * LEVEL_SCALE, z * LEVEL_SCALE]), obstacles: level.obstacles.map(([x, z, w, d, h]) => [x * LEVEL_SCALE, z * LEVEL_SCALE, w * LEVEL_SCALE, d * LEVEL_SCALE, h]) }));
  // A locked door is a real partition, not a decorative panel that can be driven around.
  // Complete the wall on both sides of every doorway while leaving the door-sized opening.
  levels.forEach((level) => {
    if (!level.door) return;
    const [x, z, w, d] = level.door;
    if (w > d) {
      const leftWidth = x - w / 2 + ARENA_HALF_WIDTH, rightWidth = ARENA_HALF_WIDTH - (x + w / 2);
      if (leftWidth > .05) level.obstacles.push([-ARENA_HALF_WIDTH + leftWidth / 2, z, leftWidth, d, 1.8]);
      if (rightWidth > .05) level.obstacles.push([x + w / 2 + rightWidth / 2, z, rightWidth, d, 1.8]);
    } else {
      const nearDepth = z - d / 2 + ARENA_HALF_DEPTH, farDepth = ARENA_HALF_DEPTH - (z + d / 2);
      if (nearDepth > .05) level.obstacles.push([x, -ARENA_HALF_DEPTH + nearDepth / 2, w, nearDepth, 1.8]);
      if (farDepth > .05) level.obstacles.push([x, z + d / 2 + farDepth / 2, w, farDepth, 1.8]);
    }
  });
  const isTouch = matchMedia('(pointer: coarse)').matches || navigator.maxTouchPoints > 0;
  root.classList.toggle('is-touch', isTouch);
  let soundEnabled = true;
  const sounds = Object.fromEntries(['mission-start', 'laser', 'pickup', 'level-complete'].map((name) => [name, new Audio(`${root.dataset.audioBase}${name}.m4a`)]));
  const playSound = (name) => { if (!soundEnabled) return; const sound = sounds[name]; sound.currentTime = 0; sound.play().catch(() => {}); };
  let musicEnabled = true, audioContext, musicTimer, musicStep = 0, lastDalekSpeech = -Infinity;
  const AudioContextClass = window.AudioContext || window.webkitAudioContext;
  const ensureAudioContext = () => { if (!AudioContextClass) return undefined; audioContext ||= new AudioContextClass(); audioContext.resume().catch(() => {}); return audioContext; };
  const technoNotes = [36, 36, 43, 36, 39, 36, 46, 43, 36, 48, 43, 39, 36, 43, 46, 34];
  const tone = (frequency, start, duration, type, volume, cutoff = 1800) => { const oscillator = audioContext.createOscillator(), gain = audioContext.createGain(), filter = audioContext.createBiquadFilter(); oscillator.type = type; oscillator.frequency.setValueAtTime(frequency, start); filter.type = 'lowpass'; filter.frequency.setValueAtTime(cutoff, start); gain.gain.setValueAtTime(volume, start); gain.gain.exponentialRampToValueAtTime(.0001, start + duration); oscillator.connect(filter).connect(gain).connect(audioContext.destination); oscillator.start(start); oscillator.stop(start + duration); };
  const noiseHit = (start, duration, volume, highpass) => { const frames = Math.ceil(audioContext.sampleRate * duration), buffer = audioContext.createBuffer(1, frames, audioContext.sampleRate), data = buffer.getChannelData(0); for (let i = 0; i < frames; i += 1) data[i] = Math.random() * 2 - 1; const source = audioContext.createBufferSource(), filter = audioContext.createBiquadFilter(), gain = audioContext.createGain(); source.buffer = buffer; filter.type = 'highpass'; filter.frequency.value = highpass; gain.gain.setValueAtTime(volume, start); gain.gain.exponentialRampToValueAtTime(.0001, start + duration); source.connect(filter).connect(gain).connect(audioContext.destination); source.start(start); };
  const scheduleTechno = () => { if (!audioContext || !musicEnabled) return; const now = audioContext.currentTime + .03, beat = .125, intensity = 1 + levelIndex * .025; for (let i = 0; i < 8; i += 1) { const step = musicStep + i, at = now + i * beat, midi = technoNotes[step % technoNotes.length]; if (step % 4 === 0) { tone(58, at, .16, 'sine', .13, 180); tone(95, at, .08, 'triangle', .07, 260); } if (step % 4 === 2) noiseHit(at, .08, .035, 1200); noiseHit(at, .025, step % 2 ? .018 : .028, 6500); tone(440 * 2 ** ((midi - 69) / 12), at, .105, 'sawtooth', .035 * intensity, 720 + levelIndex * 90); if (step % 8 === 6) tone(440 * 2 ** ((midi + 12 - 69) / 12), at, .07, 'square', .015, 2200); } musicStep += 8; };
  const playSpiderSound = (cue = 'lunge') => {
    if (!soundEnabled) return; const context = ensureAudioContext(); if (!context) return; const now = context.currentTime + .01;
    if (cue === 'skitter') { [0, .035, .075, .12, .17, .225].forEach((offset, i) => { noiseHit(now + offset, .018, .014 + (i % 2) * .006, 4200 + i * 380); tone(1720 + (i % 3) * 310, now + offset, .026, 'square', .01, 6400); }); return; }
    if (cue === 'impact') { noiseHit(now, .16, .065, 650); [520, 390, 270].forEach((frequency, i) => tone(frequency, now + i * .055, .11, 'sawtooth', .035, 1200)); return; }
    if (cue === 'shutdown') { noiseHit(now, .38, .048, 1100); [1180, 910, 640, 370].forEach((frequency, i) => tone(frequency, now + i * .085, .14, i % 2 ? 'square' : 'sawtooth', .03, 2600)); return; }
    [0, .05, .105, .17, .245].forEach((offset, i) => { noiseHit(now + offset, .04, .026 + i * .004, 3000 + i * 620); tone(980 + i * 310, now + offset, .055, i % 2 ? 'square' : 'sawtooth', .02, 5600); });
  };
  const playDalekSentry = () => { if (!soundEnabled) return; const context = ensureAudioContext(); if (!context) return; const now = context.currentTime + .01; [720, 1280, 860, 1640, 620].forEach((frequency, i) => tone(frequency, now + i * .055, .09, i % 2 ? 'square' : 'sawtooth', .035, 2600)); noiseHit(now, .32, .026, 900); };
  const playLaserShot = (charge) => { if (!soundEnabled) return; if (charge < .12) { playSound('laser'); return; } const context = ensureAudioContext(); if (!context) return; const now = context.currentTime + .01, pitch = 310 - charge * 190, duration = .16 + charge * .34; tone(pitch, now, duration, 'sawtooth', .08 + charge * .08, 950 - charge * 420); tone(pitch * .49, now, duration * 1.08, 'sine', .09 + charge * .09, 620); noiseHit(now, .08 + charge * .08, .04 + charge * .045, 180 + charge * 120); };
  const speakDalek = () => { if (!soundEnabled || !('speechSynthesis' in window) || elapsed - lastDalekSpeech < 3.2) return; lastDalekSpeech = elapsed; window.speechSynthesis.cancel(); const warning = new SpeechSynthesisUtterance('Exterminate!'); warning.rate = .72; warning.pitch = .28; warning.volume = .78; const voice = window.speechSynthesis.getVoices().find((candidate) => /^en[-_]/i.test(candidate.lang)); if (voice) warning.voice = voice; window.speechSynthesis.speak(warning); };
  const startMusic = () => { if (!musicEnabled || !ensureAudioContext()) return; if (!musicTimer) { scheduleTechno(); musicTimer = setInterval(scheduleTechno, 1000); } };
  const stopMusic = () => { clearInterval(musicTimer); musicTimer = undefined; };

  scene.add(new THREE.HemisphereLight(0xa5edff, 0x101820, 2.5)); const sun = new THREE.DirectionalLight(0xffffff, 3.5); sun.position.set(5, 12, 7); sun.castShadow = true; scene.add(sun);
  const mat = (color, emissive = 0, metalness = .25) => new THREE.MeshStandardMaterial({ color, emissive, emissiveIntensity: emissive ? 1.8 : 0, roughness: .5, metalness });
  const mesh = (geometry, material, parent, x = 0, y = 0, z = 0) => { const part = new THREE.Mesh(geometry, material); part.position.set(x, y, z); part.castShadow = true; part.receiveShadow = true; parent.add(part); return part; };
  const floor = mesh(new THREE.PlaneGeometry(ARENA_HALF_WIDTH * 2, ARENA_HALF_DEPTH * 2), mat(0x172734), scene); floor.rotation.x = -Math.PI / 2;
  const grid = new THREE.GridHelper(ARENA_HALF_WIDTH * 2, 32, 0x2ca4bb, 0x284452); grid.position.y = .01; grid.scale.z = ARENA_HALF_DEPTH / ARENA_HALF_WIDTH; scene.add(grid);
  const box = (x, z, w, d, h, color = 0x405363, collision = true, dynamic = false) => { const b = mesh(new THREE.BoxGeometry(w, h, d), mat(color), scene, x, h / 2, z); if (collision) obstacles.push({ x, z, w: w / 2, d: d / 2 }); if (dynamic) levelParts.push(b); return b; };
  box(0, -ARENA_HALF_DEPTH, ARENA_HALF_WIDTH * 2, .3, 2.2, 0x263746, false); box(0, ARENA_HALF_DEPTH, ARENA_HALF_WIDTH * 2, .3, 2.2, 0x263746, false); box(-ARENA_HALF_WIDTH, 0, .3, ARENA_HALF_DEPTH * 2, 2.2, 0x263746, false); box(ARENA_HALF_WIDTH, 0, .3, ARENA_HALF_DEPTH * 2, 2.2, 0x263746, false);

  const gate = new THREE.Group(); [-1.45, 1.45].forEach((x) => mesh(new THREE.BoxGeometry(.18, 2.6, .18), mat(0x18d9f3, 0x075b6a), gate, x, 1.3)); mesh(new THREE.BoxGeometry(3.1, .18, .18), mat(0x18d9f3, 0x075b6a), gate, 0, 2.5); gate.position.set(0, 0, -3.6); scene.add(gate);
  const dock = mesh(new THREE.BoxGeometry(3.2, .08, 2.6), mat(0x2bdf8a, 0x09623e), scene, 7.8, .05, 5.7);
  const keyObject = new THREE.Group(); mesh(new THREE.TorusGeometry(.22, .07, 10, 20), mat(0xffd84a, 0x8a6100), keyObject, 0, .45); mesh(new THREE.BoxGeometry(.65, .1, .1), mat(0xffd84a, 0x8a6100), keyObject, .42, .45); scene.add(keyObject);
  const doorObject = mesh(new THREE.BoxGeometry(4, 1.8, .35), mat(0xd14c42, 0x5c1714), scene); doorObject.position.y = .9;
  for (let i = 0; i < Math.max(...levels.map((level) => level.cells.length)); i += 1) { const c = mesh(new THREE.CylinderGeometry(.24, .24, .7, 14), mat(0xffc83d, 0x805000), scene); c.rotation.z = Math.PI / 2; cells.push(c); }

  const armAssemblies = [], robotRig = { treadWheels: [], finishMaterials: [], sabers: [] };
  const buildROB = () => {
    const r = new THREE.Group(), dark = mat(0x111820), body = mat(selectedFinish().color, 0, .58), steel = mat(0x45515d, 0, .75), cyan = mat(0x38dfff, 0x087995), green = mat(0x4cff76, 0x168c32), tire = mat(0x030405, 0, .8), hub = mat(0xf1a43c, 0x5b2c05, .72); robotRig.finishMaterials.push(body);
    [-1, 1].forEach((side) => {
      const tread = new THREE.Group(); tread.name = side < 0 ? 'Left Tri-Wheel Tread' : 'Right Tri-Wheel Tread'; tread.position.x = side * .83; r.add(tread);
      mesh(new THREE.BoxGeometry(.46, .15, 1.58), tire, tread, 0, .18);
      [-1, 1].forEach((end) => { const belt = mesh(new THREE.BoxGeometry(.46, .15, .78), tire, tread, 0, .535, end * .34); belt.rotation.x = end * .43; });
      [{ z: -.68, y: .39, radius: .28 }, { z: 0, y: .7, radius: .32 }, { z: .68, y: .39, radius: .28 }].forEach(({ z, y, radius }, index) => {
        const wheel = new THREE.Group(); wheel.name = `${side < 0 ? 'Left' : 'Right'} Tri-Wheel ${index + 1}`; wheel.position.set(0, y, z); tread.add(wheel); robotRig.treadWheels.push({ wheel, side: side < 0 ? 'left' : 'right' });
        const rubber = mesh(new THREE.CylinderGeometry(radius, radius, .48, 24), tire, wheel); rubber.rotation.z = Math.PI / 2;
        const rim = mesh(new THREE.CylinderGeometry(radius * .67, radius * .67, .5, 20), steel, wheel); rim.rotation.z = Math.PI / 2;
        const cap = mesh(new THREE.CylinderGeometry(radius * .24, radius * .24, .52, 16), hub, wheel); cap.rotation.z = Math.PI / 2;
      });
    });
    mesh(new THREE.BoxGeometry(1.35, .3, 1.08), body, r, 0, .57);
    const torso = new THREE.Group(); torso.name = 'Torso Assembly'; r.add(torso); robotRig.torso = torso;
    mesh(new THREE.BoxGeometry(1.25, 1.25, 1.55), body, torso, 0, .95);
    [-.31, .31].forEach((x, i) => { const ring = mesh(new THREE.TorusGeometry(.2, .055, 10, 24), i ? cyan : mat(0x3299ff, 0x0b477e), torso, x, 1.08, -.79); mesh(new THREE.CylinderGeometry(.135, .135, .025, 24), mat(0x071019), torso, x, 1.08, -.805).rotation.x = Math.PI / 2; });
    mesh(new THREE.BoxGeometry(.22, .12, .07), steel, torso, 0, .79, -.81); mesh(new THREE.SphereGeometry(.045, 10, 8), green, torso, 0, .79, -.86);
    mesh(new THREE.CylinderGeometry(.08, .11, .72, 10), steel, torso, 0, 1.87); const head = mesh(new THREE.SphereGeometry(.35, 18, 12), body, torso, 0, 2.3, -.02); head.scale.z = .82;
    mesh(new THREE.SphereGeometry(.07, 12, 8), green, torso, 0, 2.29, -.34); [-.16, .16].forEach((x) => { const eye = mesh(new THREE.CylinderGeometry(.07, .07, .09, 12), cyan, torso, x, 2.36, -.31); eye.rotation.x = Math.PI / 2; const antenna = mesh(new THREE.CylinderGeometry(.018, .018, .55, 8), steel, torso, x * 1.5, 2.77); antenna.rotation.z = x > 0 ? -.3 : .3; });
    [-1, 1].forEach((side) => { const arm = new THREE.Group(); arm.name = side < 0 ? 'Left Arm Assembly' : 'Right Arm Assembly'; arm.position.set(side * .7, 1.45, 0); arm.userData.side = side; torso.add(arm); armAssemblies.push(arm); let x = 0, y = 0, z = 0; for (let j = 0; j < 7; j += 1) { mesh(new THREE.SphereGeometry(j < 3 ? .1 : .07, 12, 8), j % 2 ? body : steel, arm, x, y, z); if (j < 6) { const link = mesh(new THREE.BoxGeometry(.13, .28, .13), steel, arm, x, y - .15, z - .04); link.rotation.z = side * (j < 2 ? -.2 : .08); } x += side * .045; y -= j < 3 ? .25 : .09; z -= j < 3 ? .04 : .12; } const grip = mesh(new THREE.CylinderGeometry(.055, .055, .28, 12), dark, arm, x, Math.max(-.9, y), z - .12); grip.rotation.x = Math.PI / 2; const saber = mesh(new THREE.CylinderGeometry(.035, .035, 1.55, 10), side < 0 ? green : cyan, arm, x, Math.max(-.9, y), z - .92); saber.rotation.x = Math.PI / 2; saber.visible = false; robotRig.sabers.push(saber); });
    const gatling = new THREE.Group(); gatling.name = 'Right Shoulder Gatling'; gatling.position.set(.78, 1.65, -.1); torso.add(gatling); robotRig.gatling = gatling;
    mesh(new THREE.BoxGeometry(.38, .3, .58), mat(0x030405, 0, .85), gatling, 0, 0, -.1);
    const barrelCluster = new THREE.Group(); gatling.add(barrelCluster); robotRig.barrels = barrelCluster; [-.085, .085].forEach((x) => [-.085, .085].forEach((y) => { const barrel = mesh(new THREE.CylinderGeometry(.025, .025, .82, 10), mat(0x000000, 0, .95), barrelCluster, x, y, -.56); barrel.rotation.x = Math.PI / 2; }));
    const lockLamp = mesh(new THREE.SphereGeometry(.085, 12, 8), mat(0xff182f, 0xff0018, .5), gatling, 0, .23, -.32); lockLamp.visible = false; robotRig.lockLamp = lockLamp;
    const twinBlasters = new THREE.Group(); twinBlasters.name = 'Twin Blasters'; torso.add(twinBlasters); robotRig.twinBlasters = twinBlasters;
    [-.62, .62].forEach((x) => { mesh(new THREE.BoxGeometry(.18, .16, .45), dark, twinBlasters, x, 1.25, -.42); const barrel = mesh(new THREE.CylinderGeometry(.035, .045, .58, 10), cyan, twinBlasters, x, 1.25, -.78); barrel.rotation.x = Math.PI / 2; });
    const arcCannon = new THREE.Group(); arcCannon.name = 'Arc Cannon'; arcCannon.position.set(.78, 1.65, -.1); torso.add(arcCannon); robotRig.arcCannon = arcCannon;
    mesh(new THREE.BoxGeometry(.46, .38, .62), dark, arcCannon, 0, 0, -.12); const arcBarrel = mesh(new THREE.CylinderGeometry(.09, .15, .92, 12), mat(0x8c63ff, 0x3b1ca8), arcCannon, 0, 0, -.68); arcBarrel.rotation.x = Math.PI / 2;
    const hammer = new THREE.Group(); hammer.name = 'Power Hammer'; hammer.position.set(.82, .5, -.68); torso.add(hammer); robotRig.hammer = hammer; const hammerHandle = mesh(new THREE.CylinderGeometry(.045, .055, 1.15, 10), steel, hammer); hammerHandle.rotation.z = -.28; mesh(new THREE.BoxGeometry(.65, .28, .28), mat(0xf1a43c, 0x5b2c05), hammer, .16, .5, 0);
    return r;
  };
  const robot = buildROB(); scene.add(robot);

  const buildSpider = () => { const g = new THREE.Group(), bone = mat(0xe6dcb9), steel = mat(0x697178, 0, .7); mesh(new THREE.BoxGeometry(1.5, .45, 1.25), steel, g, 0, .72); const skull = mesh(new THREE.DodecahedronGeometry(.52, 0), bone, g, 0, 1.16, -.45); skull.scale.z = 1.25; [-1, 1].forEach((side) => { for (let row = -1; row <= 1; row += 2) { const z = row * .45; const hip = mesh(new THREE.BoxGeometry(.95, .13, .2), steel, g, side * .75, .65, z); hip.rotation.z = side * -.35; const shin = mesh(new THREE.BoxGeometry(.17, .75, .22), bone, g, side * 1.35, .35, z); shin.rotation.z = side * .3; } }); return g; };
  const buildDalek = () => { const g = new THREE.Group(), silver = mat(0xaeb9c4, 0, .75), blue = mat(0x279de0, 0x063b61); const skirt = mesh(new THREE.CylinderGeometry(.65, 1.05, 1.55, 8), silver, g, 0, .82); for (let y = .35; y < 1.25; y += .3) for (let a = 0; a < Math.PI * 2; a += Math.PI / 3) mesh(new THREE.SphereGeometry(.11, 10, 8), blue, g, Math.sin(a) * (.78 - y * .08), y, Math.cos(a) * (.78 - y * .08)); mesh(new THREE.CylinderGeometry(.65, .65, .5, 16), mat(0x242b31), g, 0, 1.75); mesh(new THREE.SphereGeometry(.62, 16, 10, 0, Math.PI * 2, 0, Math.PI / 2), silver, g, 0, 2); const eye = mesh(new THREE.CylinderGeometry(.055, .085, .9, 10), silver, g, 0, 2.2, -.63); eye.rotation.x = Math.PI / 2; mesh(new THREE.SphereGeometry(.12, 10, 8), blue, g, 0, 2.2, -1.08); [-1, 1].forEach((side) => { const arm = mesh(new THREE.CylinderGeometry(.035, .05, 1, 8), silver, g, side * .48, 1.6, -.65); arm.rotation.x = Math.PI / 2; }); return g; };
  for (let i = 0; i < 8; i += 1) { const spider = buildSpider(); spider.userData.type = 'spider'; scene.add(spider); enemies.push(spider); const dalek = buildDalek(); dalek.userData.type = 'dalek'; scene.add(dalek); enemies.push(dalek); }

  const ui = { time: root.querySelector('[data-sim-time]'), score: root.querySelector('[data-sim-score]'), level: root.querySelector('[data-sim-level]'), levelName: root.querySelector('[data-sim-level-name]'), left: root.querySelector('[data-sim-left]'), right: root.querySelector('[data-sim-right]'), enemies: root.querySelector('[data-sim-enemies]'), lock: root.querySelector('[data-sim-lock]'), message: root.querySelector('[data-sim-message]'), start: root.querySelector('[data-sim-start]'), reset: root.querySelector('[data-sim-reset]'), fullscreen: root.querySelector('[data-sim-fullscreen]'), laserButtons: [...root.querySelectorAll('[data-sim-laser]')], saberButtons: [...root.querySelectorAll('[data-sim-saber]')], health: root.querySelector('[data-sim-health]'), healthText: root.querySelector('[data-sim-health-text]'), boss: root.querySelector('[data-sim-boss]'), bossName: root.querySelector('[data-sim-boss-name]'), bossText: root.querySelector('[data-sim-boss-text]'), bossHealth: root.querySelector('[data-sim-boss-health]'), progress: root.querySelector('[data-sim-progress]'), nextUnlock: root.querySelector('[data-sim-next-unlock]'), finish: root.querySelector('[data-sim-finish]'), ranged: root.querySelector('[data-sim-ranged]'), melee: root.querySelector('[data-sim-melee]'), loadoutStatus: root.querySelector('[data-sim-loadout-status]') };
  const objectives = Object.fromEntries([...root.querySelectorAll('[data-objective]')].map((item) => [item.dataset.objective, item])); const say = (t) => { ui.message.textContent = t; }; const mark = (n, t, p) => { if (objectives[n].classList.contains('is-complete')) return; objectives[n].classList.add('is-complete'); score += p; say(t); };
  const currentBoss = () => enemies.find((enemy) => enemy.userData.alive && enemy.userData.isBoss);
  const updateWorkshop = () => {
    selectedFinishID = selectedFinish().id; selectedRangedID = selectedRanged().id; selectedMeleeID = selectedMelee().id;
    ui.progress.textContent = `${highestCompletedLevel} / ${levels.length} levels complete`;
    ui.nextUnlock.textContent = highestCompletedLevel < 5 ? 'Next unlock: Twin Blasters after Level 5.' : highestCompletedLevel < 10 ? 'Next unlock: Power Hammer after Level 10.' : highestCompletedLevel < 15 ? 'Next unlock: Arc Cannon after Level 15.' : 'Every workshop weapon is unlocked.';
    ui.finish.value = selectedFinishID; ui.ranged.value = selectedRangedID; ui.melee.value = selectedMeleeID;
    [...ui.ranged.options].forEach((option) => { const weapon = rangedWeapons.find(({ id }) => id === option.value); option.disabled = Boolean(weapon && !isUnlocked(weapon, highestCompletedLevel)); });
    [...ui.melee.options].forEach((option) => { const weapon = meleeWeapons.find(({ id }) => id === option.value); option.disabled = Boolean(weapon && !isUnlocked(weapon, highestCompletedLevel)); });
    ui.loadoutStatus.textContent = `${selectedFinish().name} finish · ${selectedRanged().name} · ${selectedMelee().name}`;
  };
  const applyLoadout = () => {
    robotRig.finishMaterials.forEach((material) => material.color.setHex(selectedFinish().color));
    robotRig.gatling.visible = selectedRangedID === 'shoulderGatling'; robotRig.twinBlasters.visible = selectedRangedID === 'twinBlasters'; robotRig.arcCannon.visible = selectedRangedID === 'arcCannon'; robotRig.hammer.visible = selectedMeleeID === 'powerHammer';
    robotRig.sabers.forEach((saber) => { saber.visible = selectedMeleeID === 'dualSabers' && Boolean(saberAnimation); });
    ui.laserButtons.forEach((button) => { button.setAttribute('aria-label', `Hold to charge ROB's ${selectedRanged().name}`); });
    ui.saberButtons.forEach((button) => { button.setAttribute('aria-label', `Attack with ROB's ${selectedMelee().name}`); });
    updateWorkshop();
  };
  const loadLevel = (index) => {
    const level = levels[index];
    levelParts.splice(0).forEach((part) => scene.remove(part)); obstacles.length = 0;
    level.obstacles.forEach((o, i) => box(...o, i % 2 ? 0x465262 : 0x344552, true, true));
    floor.material.color.setHex(level.floor); grid.material.color.setHex(level.grid); gate.position.set(level.gate[0], 0, level.gate[1]); dock.position.set(level.dock[0], .05, level.dock[1]);
    robot.position.set(0, 0, ARENA_HALF_DEPTH - 1.6); robot.rotation.set(0, 0, 0); robotRig.torso.rotation.set(0, 0, 0); armAssemblies.forEach((arm) => arm.rotation.set(0, 0, 0)); levelElapsed = 0; health = MAX_ROB_HEALTH; damageInvulnerableUntil = -Infinity; gateDone = false; cellCount = 0; levelComplete = false; hasKey = false; doorOpen = !level.key; laserLock = undefined; laserChargeStarted = undefined; saberCombo = 0; lastSaberAttack = -Infinity; saberAnimation = undefined; gamepadLaserHeld = false;
    releaseAllInput(); keyObject.visible = Boolean(level.key); if (level.key) keyObject.position.set(level.key[0], 0, level.key[1]);
    doorObject.visible = Boolean(level.door); if (level.door) { doorObject.position.set(level.door[0], .9, level.door[1]); doorObject.scale.set(level.door[2] / 4, 1, level.door[3] / .35); }
    bolts.splice(0).forEach((bolt) => scene.remove(bolt)); enemyBolts.splice(0).forEach((bolt) => scene.remove(bolt)); window.speechSynthesis?.cancel?.();
    cells.forEach((cell, i) => { const position = level.cells[i]; cell.visible = Boolean(position); cell.userData.got = false; if (position) cell.position.set(position[0], .55, position[1]); });
    enemies.forEach((enemy) => { enemy.visible = false; enemy.userData.alive = false; });
    level.enemies.forEach((spec, enemyIndex) => {
      const enemy = enemies.find((candidate) => !candidate.visible && candidate.userData.type === spec[0]);
      if (!enemy) return;
      const stats = enemyIndex === 0 ? bossStats(index + 1, level.health) : { isBoss: false, shields: level.health };
      enemy.visible = true; enemy.userData.alive = true; enemy.userData.isBoss = stats.isBoss; enemy.userData.health = stats.shields; enemy.userData.maxHealth = stats.shields; enemy.userData.contactDamage = stats.contactDamage || (spec[0] === 'spider' ? 6 : 5); enemy.userData.projectileDamage = stats.projectileDamage || 4; enemy.userData.name = `${stats.isBoss ? 'Boss ' : ''}${spec[0] === 'spider' ? 'Spider bot' : 'Dalek-style sentry robot'}`; enemy.scale.setScalar(stats.isBoss ? 1.35 : 1);
      enemy.position.set(spec[1], 0, spec[2]); enemy.userData.origin = enemy.position.clone(); enemy.userData.patrolPhase = enemyIndex * 2.17; enemy.userData.nextAttack = elapsed + 1.6 + enemyIndex * .65; enemy.userData.nextSkitterSound = elapsed + .8 + enemyIndex * .38; enemy.userData.lungeUntil = 0;
    });
    Object.values(objectives).forEach((objective) => objective.classList.remove('is-complete')); objectives.cells.querySelector('[data-objective-text]').textContent = `Collect ${level.cells.length} energy cells`; objectives.enemies.querySelector('[data-objective-text]').textContent = `Disable ${level.enemies.length} hostile robots`; objectives.dock.querySelector('[data-objective-text]').textContent = level.key ? 'Find the key, open the door, then dock' : 'Reach the dock after the room is safe';
    ui.level.textContent = `${index + 1} / ${levels.length}`; ui.levelName.textContent = `Level ${index + 1} · ${level.name}`; ui.start.hidden = false; ui.start.textContent = index ? `Start level ${index + 1}` : 'Begin campaign'; applyLoadout(); say(`${level.name}: ${index ? 'difficulty increased' : 'systems ready'}.`);
  };
  const reset = () => { running = complete = false; elapsed = score = 0; levelIndex = 0; root.dispatchEvent(new CustomEvent('rob:campaign-reset')); loadLevel(0); };
  const circleHitsBox = (p, o, radius = .48) => { const dx = p.x - THREE.MathUtils.clamp(p.x, o.x - o.w, o.x + o.w), dz = p.z - THREE.MathUtils.clamp(p.z, o.z - o.d, o.z + o.d); return dx * dx + dz * dz < radius * radius; };
  const robotAxes = (heading) => ({ right: { x: Math.cos(heading), z: -Math.sin(heading) }, length: { x: Math.sin(heading), z: Math.cos(heading) } });
  const robotHitsBox = (p, heading, o) => {
    const { right, length } = robotAxes(heading), delta = { x: o.x - p.x, z: o.z - p.z }, axes = [right, length, { x: 1, z: 0 }, { x: 0, z: 1 }];
    return axes.every((axis) => {
      const distance = Math.abs(delta.x * axis.x + delta.z * axis.z), robotRadius = ROBOT_HALF_WIDTH * Math.abs(right.x * axis.x + right.z * axis.z) + ROBOT_HALF_LENGTH * Math.abs(length.x * axis.x + length.z * axis.z), boxRadius = o.w * Math.abs(axis.x) + o.d * Math.abs(axis.z);
      return distance < robotRadius + boxRadius;
    });
  };
  const robotHitsCircle = (p, heading, center, radius) => { const { right, length } = robotAxes(heading), dx = center.x - p.x, dz = center.z - p.z, localX = dx * right.x + dz * right.z, localZ = dx * length.x + dz * length.z, nearestX = THREE.MathUtils.clamp(localX, -ROBOT_HALF_WIDTH, ROBOT_HALF_WIDTH), nearestZ = THREE.MathUtils.clamp(localZ, -ROBOT_HALF_LENGTH, ROBOT_HALF_LENGTH), gapX = localX - nearestX, gapZ = localZ - nearestZ; return gapX * gapX + gapZ * gapZ < radius * radius; };
  const collision = (p, heading = robot.rotation.y) => { const { right, length } = robotAxes(heading), extentX = ROBOT_HALF_WIDTH * Math.abs(right.x) + ROBOT_HALF_LENGTH * Math.abs(length.x), extentZ = ROBOT_HALF_WIDTH * Math.abs(right.z) + ROBOT_HALF_LENGTH * Math.abs(length.z), door = levels[levelIndex].door, doorBox = door && { x: door[0], z: door[1], w: door[2] / 2, d: door[3] / 2 }; return Math.abs(p.x) + extentX > ARENA_HALF_WIDTH - ARENA_CLEARANCE || Math.abs(p.z) + extentZ > ARENA_HALF_DEPTH - ARENA_CLEARANCE || obstacles.some((o) => robotHitsBox(p, heading, o)) || (!doorOpen && doorBox && robotHitsBox(p, heading, doorBox)); };
  const enemyCollision = (p, radius) => Math.abs(p.x) > ARENA_HALF_WIDTH - .75 || Math.abs(p.z) > ARENA_HALF_DEPTH - .75 || obstacles.some((o) => circleHitsBox(p, o, radius)) || (!doorOpen && levels[levelIndex].door && circleHitsBox(p, { x: levels[levelIndex].door[0], z: levels[levelIndex].door[1], w: levels[levelIndex].door[2] / 2, d: levels[levelIndex].door[3] / 2 }, radius));
  const projectileBlockers = () => {
    const blockers = [...obstacles, { x: 0, z: -ARENA_HALF_DEPTH, w: ARENA_HALF_WIDTH, d: .09 }, { x: 0, z: ARENA_HALF_DEPTH, w: ARENA_HALF_WIDTH, d: .09 }, { x: -ARENA_HALF_WIDTH, z: 0, w: .09, d: ARENA_HALF_DEPTH }, { x: ARENA_HALF_WIDTH, z: 0, w: .09, d: ARENA_HALF_DEPTH }];
    const door = levels[levelIndex].door; if (!doorOpen && door) blockers.push({ x: door[0], z: door[1], w: door[2] / 2, d: door[3] / 2 });
    return blockers;
  };
  const moveEnemy = (enemy, target, speed, dt) => { const offset = target.clone().sub(enemy.position); offset.y = 0; const distance = offset.length(); if (distance < .04) return false; const step = Math.min(distance, speed * dt), direction = offset.normalize(), proposedX = enemy.position.clone(), proposedZ = enemy.position.clone(), radius = enemy.userData.type === 'spider' ? .72 : .64; proposedX.x += direction.x * step; proposedZ.z += direction.z * step; let moved = false; if (!enemyCollision(proposedX, radius)) { enemy.position.x = proposedX.x; moved = true; } if (!enemyCollision(proposedZ, radius)) { enemy.position.z = proposedZ.z; moved = true; } if (!moved) enemy.userData.patrolPhase += .9; return moved; };
  const damageEnemy = (hit, weapon, amount = 1) => { hit.userData.health -= amount; score += 50 * amount; if (hit.userData.type === 'spider') playSpiderSound(hit.userData.health <= 0 ? 'shutdown' : 'impact'); say(`${hit.userData.name} hit by ${weapon} — ${Math.max(0, hit.userData.health)} shields remain.`); if (hit.userData.health <= 0) { hit.userData.alive = false; hit.visible = false; score += hit.userData.isBoss ? 1000 : 300; if (laserLock === hit) laserLock = undefined; if (hit.userData.type !== 'spider') playDalekSentry(); say(`${hit.userData.name} disabled by ${weapon}. ${enemies.filter((enemy) => enemy.userData.alive).length} targets remain.`); if (enemies.every((e) => !e.userData.alive)) mark('enemies', 'Training-room defense complete.', 400); } };
  const damageROB = (attack, damage) => {
    if (!running || elapsed < damageInvulnerableUntil) return false;
    const result = applyROBHealthDamage(health, damage); health = result.health; score = Math.max(0, score - result.scorePenalty); damageInvulnerableUntil = elapsed + .75;
    if (health > 0) { say(`${attack} dealt ${result.appliedDamage} damage. ROB health: ${health}/${MAX_ROB_HEALTH}.`); return true; }
    loadLevel(levelIndex); running = true; ui.start.hidden = true; damageInvulnerableUntil = elapsed + 1; say(`ROB was disabled by ${attack}. Restarting level ${levelIndex + 1} with full health.`); return true;
  };
  const combatNow = () => performance.now() / 1000;
  const laserChargeAmount = () => laserChargeStarted === undefined ? 0 : THREE.MathUtils.clamp((combatNow() - laserChargeStarted) / 1.5, 0, 1);
  const scanForLaserTarget = () => {
    if (!running || complete) { laserLock = undefined; return; }
    const active = enemies.filter((enemy) => enemy.userData.alive && enemy.visible).map((enemy) => ({ enemy, distance: enemy.position.distanceTo(robot.position) })).filter(({ distance }) => distance <= 28).sort((a, b) => a.distance - b.distance);
    laserLock = active[0]?.enemy;
  };
  const fireLaser = (charge = 0) => {
    scanForLaserTarget(); if (!running || complete || elapsed - lastShot < .25) return;
    const weapon = selectedRanged(); if (!laserLock) { say(`${weapon.name} scanning — no enemy lock yet.`); return; }
    lastShot = elapsed; const start = new THREE.Vector3(weapon.id === 'twinBlasters' ? -.62 : .78, weapon.id === 'twinBlasters' ? 1.25 : 1.65, -.58).applyQuaternion(robot.quaternion).add(robot.position), target = laserLock.position.clone().add(new THREE.Vector3(0, 1, 0)), direction = target.sub(start).normalize(), radius = .04 + charge * .11 + (weapon.id === 'arcCannon' ? .06 : 0), length = .95 + charge * 1.9;
    const color = weapon.id === 'arcCannon' ? 0xa66cff : weapon.id === 'twinBlasters' ? 0x38dfff : 0xff1838;
    playLaserShot(charge); const bolt = mesh(new THREE.CylinderGeometry(radius, radius, length, 12), mat(color, color), scene); bolt.quaternion.setFromUnitVectors(new THREE.Vector3(0, 1, 0), direction); bolt.position.copy(start).addScaledVector(direction, .7 + charge * .55); bolt.userData.velocity = direction.multiplyScalar(weapon.projectileSpeed + charge * 3.5); bolt.userData.damage = weaponDamage(weapon, charge); bolt.userData.charge = charge; bolt.userData.weapon = weapon; bolts.push(bolt); say(charge > .72 ? `Charged ${weapon.name} blast fired at ${laserLock.userData.name}!` : `${weapon.name} lock confirmed — projectile fired at ${laserLock.userData.name}.`);
  };
  const beginLaserCharge = () => { if (!running || complete || laserChargeStarted !== undefined) return; scanForLaserTarget(); laserChargeStarted = combatNow(); if (laserLock) say(`Red lock: ${laserLock.userData.name}. Hold to charge the ${selectedRanged().name}.`); else say(`${selectedRanged().name} scanning for a target…`); };
  const releaseLaserCharge = () => { if (laserChargeStarted === undefined) return; const charge = laserChargeAmount(); laserChargeStarted = undefined; fireLaser(charge); };
  const cancelLaserCharge = () => { laserChargeStarted = undefined; ui.laserButtons.forEach((button) => button.classList.remove('is-charging')); };
  const enemyBoltGeometry = new THREE.CylinderGeometry(.055, .055, 1.05, 8), enemyBoltMaterial = mat(0x70c8ff, 0x194ec4);
  const fireEnemyLaser = (enemy) => { const start = enemy.position.clone().add(new THREE.Vector3(0, enemy.userData.type === 'dalek' ? 1.55 : .9, 0)), target = robot.position.clone().add(new THREE.Vector3(0, .9, 0)), direction = target.sub(start).normalize(), bolt = mesh(enemyBoltGeometry, enemyBoltMaterial, scene); bolt.quaternion.setFromUnitVectors(new THREE.Vector3(0, 1, 0), direction); bolt.position.copy(start).addScaledVector(direction, .85); bolt.userData.velocity = direction.multiplyScalar(7.5 + levelIndex * .18); bolt.userData.damage = enemy.userData.projectileDamage; bolt.userData.sourceName = `${enemy.userData.name} laser`; enemyBolts.push(bolt); };
  const saberSlash = () => {
    if (!running || complete || saberAnimation) return;
    const now = combatNow(), weapon = selectedMelee(), forward = new THREE.Vector3(0, 0, -1).applyQuaternion(robot.quaternion).normalize(), origin = { x: robot.position.x, z: robot.position.z };
    if (weapon.id === 'powerHammer') {
      const radius = 3.8, target = { x: origin.x + forward.x * radius, z: origin.z + forward.z * radius };
      if (!meleeAnimationIsClear({ origin, target, blockers: projectileBlockers(), padding: .2 })) { saberCombo = 0; say('Power hammer blocked by the wall. Back up before swinging.'); return; }
      saberCombo = 0; lastSaberAttack = now; saberAnimation = { style: 'hammer', started: now, duration: .65 };
      const hits = enemies.filter((enemy) => enemy.userData.alive).filter((enemy) => { const offset = enemy.position.clone().sub(robot.position), enemyPoint = { x: enemy.position.x, z: enemy.position.z }; return offset.length() <= radius && offset.clone().normalize().dot(forward) > .08 && meleeAnimationIsClear({ origin, target: enemyPoint, blockers: projectileBlockers() }); });
      hits.forEach((enemy) => damageEnemy(enemy, 'power hammer', 2)); playSound('laser'); say(hits.length ? `Power hammer smash struck ${hits.length} ${hits.length === 1 ? 'target' : 'targets'}.` : 'Power hammer smash missed. Face a target within reach.'); return;
    }
    saberCombo = now - lastSaberAttack <= 1.15 ? saberCombo + 1 : 1; const style = saberCombo >= 3 ? 'spin' : saberCombo % 2 ? 'left' : 'right', radius = style === 'spin' ? 4.1 : 3.15, target = { x: origin.x + forward.x * radius, z: origin.z + forward.z * radius };
    const clear = style === 'spin' ? meleeAnimationIsClear({ origin, blockers: projectileBlockers(), spinRadius: radius }) : meleeAnimationIsClear({ origin, target, blockers: projectileBlockers(), padding: .35 });
    if (!clear) { saberCombo = 0; say('Saber swing blocked by the wall. Back up before attacking.'); return; }
    lastSaberAttack = now; if (style === 'spin') saberCombo = 0; saberAnimation = { style, started: now, duration: style === 'spin' ? .82 : .48 }; playSound('laser');
    const hits = enemies.filter((enemy) => enemy.userData.alive).filter((enemy) => { const offset = enemy.position.clone().sub(robot.position), enemyPoint = { x: enemy.position.x, z: enemy.position.z }; return offset.length() <= radius && (style === 'spin' || offset.clone().normalize().dot(forward) > -.08) && meleeAnimationIsClear({ origin, target: enemyPoint, blockers: projectileBlockers() }); }); hits.forEach((enemy) => damageEnemy(enemy, style === 'spin' ? 'dual-saber spin' : `dual-saber ${style} sweep`)); say(style === 'spin' ? `Spin attack! ROB extended both sabers and struck ${hits.length} ${hits.length === 1 ? 'enemy' : 'enemies'}.` : hits.length ? `${style === 'left' ? 'Left' : 'Right'} dual-arm sweep connected.` : `${style === 'left' ? 'Left' : 'Right'} sweep missed. Close the distance, then chain three attacks for a spin.`);
  };
  const updateRobotWeapons = () => {
    scanForLaserTarget(); const now = combatNow(), charge = laserChargeAmount(); robotRig.torso.rotation.set(0, 0, 0); armAssemblies.forEach((arm) => arm.rotation.set(0, 0, 0));
    if (saberAnimation) { const progress = THREE.MathUtils.clamp((now - saberAnimation.started) / saberAnimation.duration, 0, 1), eased = .5 - Math.cos(progress * Math.PI) / 2; if (saberAnimation.style === 'spin') { robotRig.torso.rotation.y = eased * Math.PI * 2; armAssemblies.forEach((arm) => { arm.rotation.z = arm.userData.side * Math.PI / 2; }); } else if (saberAnimation.style === 'hammer') { robotRig.hammer.rotation.x = -Math.sin(progress * Math.PI) * 1.5; } else { const direction = saberAnimation.style === 'left' ? 1 : -1, sweep = direction * (-1.18 + eased * 2.36); armAssemblies.forEach((arm) => { arm.rotation.y = sweep; arm.rotation.z = arm.userData.side * (.28 + Math.sin(progress * Math.PI) * .5); }); } if (progress >= 1) { saberAnimation = undefined; robotRig.hammer.rotation.set(0, 0, 0); } }
    robotRig.sabers.forEach((saber) => { saber.visible = selectedMeleeID === 'dualSabers' && Boolean(saberAnimation); });
    const aimingRig = selectedRangedID === 'arcCannon' ? robotRig.arcCannon : robotRig.gatling; if (laserLock) { const offset = laserLock.position.clone().sub(robot.position), worldYaw = Math.atan2(-offset.x, -offset.z), localYaw = Math.atan2(Math.sin(worldYaw - robot.rotation.y), Math.cos(worldYaw - robot.rotation.y)); aimingRig.rotation.y += (localYaw - aimingRig.rotation.y) * .18; } else aimingRig.rotation.y = Math.sin(now * 1.35) * .9;
    robotRig.barrels.rotation.z += .035 + charge * .22; robotRig.lockLamp.visible = Boolean(laserLock); robotRig.lockLamp.scale.setScalar(laserLock ? 1 + Math.sin(now * 12) * .24 : 1); if (ui.lock) { const lockLabel = laserLock ? `RED LOCK · ${laserLock.userData.name}` : 'SCANNING'; if (ui.lock.textContent !== lockLabel) ui.lock.textContent = lockLabel; ui.lock.classList.toggle('is-locked', Boolean(laserLock)); }
    ui.laserButtons.forEach((button) => { button.classList.toggle('is-charging', laserChargeStarted !== undefined); button.style.setProperty('--laser-charge', `${Math.round(charge * 100)}%`); const compact = button.classList.contains('rob-sim__fire'); button.textContent = laserChargeStarted !== undefined ? `CHARGE ${Math.round(charge * 100)}%` : laserLock ? (compact ? `${selectedRanged().shortName.toUpperCase()} LOCK` : `${selectedRanged().name} locked · hold Q`) : (compact ? `${selectedRanged().shortName.toUpperCase()} SCAN` : `${selectedRanged().name} scanning · hold Q`); });
    ui.saberButtons.forEach((button) => { button.textContent = button.classList.contains('rob-sim__fire') ? selectedMelee().shortName.toUpperCase() : `${selectedMelee().name} · Space`; });
  };
  const readInput = () => { let forward = touch.forward || (((keys.has('ArrowUp') || keys.has('KeyW')) ? 1 : 0) - ((keys.has('ArrowDown') || keys.has('KeyS')) ? 1 : 0)), steering = touch.steering || (((keys.has('ArrowLeft') || keys.has('KeyA')) ? 1 : 0) - ((keys.has('ArrowRight') || keys.has('KeyD')) ? 1 : 0)), leftTarget, rightTarget; const pad = [...(navigator.getGamepads?.() || [])].find(Boolean); if (pad && (Math.abs(pad.axes[0] || 0) > .12 || Math.abs(pad.axes[1] || 0) > .12)) { forward = -(pad.axes[1] || 0); steering = -(pad.axes[0] || 0); } leftTarget = THREE.MathUtils.clamp(forward - steering * .72, -1, 1); rightTarget = THREE.MathUtils.clamp(forward + steering * .72, -1, 1); if (touch.leftActive || touch.rightActive) { leftTarget = touch.leftActive ? touch.left : 0; rightTarget = touch.rightActive ? touch.right : 0; } const gamepadLaserPressed = Boolean(pad?.buttons.some((b, i) => (i === 0 || i === 6 || i === 7) && b.pressed)); if (gamepadLaserPressed && !gamepadLaserHeld) beginLaserCharge(); else if (!gamepadLaserPressed && gamepadLaserHeld) releaseLaserCharge(); gamepadLaserHeld = gamepadLaserPressed; controls.left += (leftTarget - controls.left) * .28; controls.right += (rightTarget - controls.right) * .28; };
  const tick = (dt) => { readInput(); if (!running || complete || levelComplete) return; elapsed += dt; levelElapsed += dt; const level = levels[levelIndex], old = robot.position.clone(), oldHeading = robot.rotation.y, left = controls.left * 3.2, right = controls.right * 3.2, linear = (left + right) / 2, yaw = (right - left) / 1.55 * dt, targetHeading = oldHeading + yaw, mid = oldHeading + yaw / 2, full = robot.position.clone(); full.x -= Math.sin(mid) * linear * dt; full.z -= Math.cos(mid) * linear * dt; if (!collision(full, targetHeading)) { robot.position.copy(full); robot.rotation.y = targetHeading; } else { const resolvedHeading = collision(old, targetHeading) ? oldHeading : targetHeading, proposedX = robot.position.clone(), proposedZ = robot.position.clone(); robot.rotation.y = resolvedHeading; proposedX.x -= Math.sin(resolvedHeading) * linear * dt; if (!collision(proposedX, resolvedHeading)) robot.position.x = proposedX.x; proposedZ.x = robot.position.x; proposedZ.z -= Math.cos(resolvedHeading) * linear * dt; if (!collision(proposedZ, resolvedHeading)) robot.position.z = proposedZ.z; } robotRig.treadWheels.forEach(({ wheel, side }) => { wheel.rotation.x -= controls[side] * dt * 7.5; }); if (robot.position.equals(old) && Math.abs(linear) > .1) say('ROB’s treads and chassis are blocked. Pivot toward open space.');
    for (let i = 0; i < enemies.length; i += 1) {
      const enemy = enemies[i]; if (!enemy.userData.alive) continue;
      const toRobot = robot.position.clone().sub(enemy.position); toRobot.y = 0; const distance = toRobot.length(), isSpider = enemy.userData.type === 'spider', aggressive = gateDone || distance < (isSpider ? 10.25 : 14), patrolAngle = elapsed * (isSpider ? .62 : .34) + enemy.userData.patrolPhase, patrolRadius = isSpider ? 2.2 : 1.7;
      let target = enemy.userData.origin.clone().add(new THREE.Vector3(Math.cos(patrolAngle) * patrolRadius, 0, Math.sin(patrolAngle) * patrolRadius)), speed = isSpider ? .52 : .38;
      if (isSpider && aggressive) {
        target = robot.position.clone(); speed = .92 + level.speed * 2.1;
        if (elapsed >= enemy.userData.nextSkitterSound) { enemy.userData.nextSkitterSound = elapsed + 2.1 + (i % 3) * .42; playSpiderSound('skitter'); }
        if (elapsed >= enemy.userData.nextAttack && distance < 4.25) { enemy.userData.lungeUntil = elapsed + .72; enemy.userData.nextAttack = elapsed + Math.max(2.5, 3.5 - levelIndex * .06); playSpiderSound('lunge'); say('Spider bot skittering into a lunge — evade or use the saber!'); }
        if (elapsed < enemy.userData.lungeUntil) speed = 2.65 + level.speed * 1.4;
      } else if (!isSpider && aggressive) {
        if (distance > 5.8) target = robot.position.clone();
        else if (distance < 3.6) target = enemy.position.clone().addScaledVector(toRobot.clone().normalize(), -2.5);
        else { const orbit = new THREE.Vector3(-toRobot.z, 0, toRobot.x).normalize().multiplyScalar(Math.sin(enemy.userData.patrolPhase) < 0 ? -1.6 : 1.6); target = enemy.position.clone().add(orbit); }
        speed = .58 + level.speed * 1.25;
        if (elapsed >= enemy.userData.nextAttack && distance < 12) { enemy.userData.nextAttack = elapsed + Math.max(1.85, 3.9 - levelIndex * .09); fireEnemyLaser(enemy); playDalekSentry(); speakDalek(); say('Dalek-style sentry robot: “Exterminate!” Incoming laser — keep moving.'); }
      }
      const facing = (aggressive ? robot.position : target).clone().sub(enemy.position); facing.y = 0; if (facing.lengthSq() > .001) enemy.rotation.y = Math.atan2(-facing.x, -facing.z);
      moveEnemy(enemy, target, speed, dt); enemy.position.y = isSpider ? Math.sin(elapsed * 10 + i) * .055 : Math.sin(elapsed * 2.4 + i) * .028;
      if (robotHitsCircle(robot.position, robot.rotation.y, enemy.position, (isSpider ? .78 : .7) * (enemy.userData.isBoss ? 1.35 : 1))) { if (isSpider) playSpiderSound('impact'); else playDalekSentry(); if (damageROB(isSpider ? `${enemy.userData.name} lunge` : `${enemy.userData.name} collision`, enemy.userData.contactDamage)) return; }
    }
    for (let i = enemyBolts.length - 1; i >= 0; i -= 1) { const bolt = enemyBolts[i], start = bolt.position.clone(); bolt.position.addScaledVector(bolt.userData.velocity, dt); const impact = firstProjectileImpact({ start: { x: start.x, z: start.z }, end: { x: bolt.position.x, z: bolt.position.z }, blockers: projectileBlockers(), targets: [{ id: 'rob', x: robot.position.x, z: robot.position.z, radius: .72 }] }); if (impact || bolt.position.distanceTo(robot.position) > 42) { scene.remove(bolt); enemyBolts.splice(i, 1); if (impact?.kind === 'target') { playDalekSentry(); if (damageROB(bolt.userData.sourceName, bolt.userData.damage)) return; } } }
    for (let i = bolts.length - 1; i >= 0; i -= 1) { const bolt = bolts[i], start = bolt.position.clone(); bolt.position.addScaledVector(bolt.userData.velocity, dt); const targets = enemies.filter((enemy) => enemy.userData.alive).map((enemy) => ({ enemy, x: enemy.position.x, z: enemy.position.z, radius: (enemy.userData.type === 'spider' ? .72 : .8) * (enemy.userData.isBoss ? 1.35 : 1) })), impact = firstProjectileImpact({ start: { x: start.x, z: start.z }, end: { x: bolt.position.x, z: bolt.position.z }, blockers: projectileBlockers(), targets }); if (impact) { scene.remove(bolt); bolts.splice(i, 1); if (impact.kind === 'wall') { say(`${bolt.userData.weapon.name} struck the wall. Reposition for a clear shot.`); continue; } const hit = impact.target.enemy, weaponLabel = bolt.userData.charge > .72 ? `charged ${bolt.userData.weapon.name.toLowerCase()}` : bolt.userData.weapon.name.toLowerCase(); damageEnemy(hit, weaponLabel, bolt.userData.damage || 1); if (bolt.userData.weapon.id === 'arcCannon') { const secondary = enemies.find((enemy) => enemy.userData.alive && enemy !== hit && enemy.position.distanceTo(hit.position) <= 2.2); if (secondary) damageEnemy(secondary, 'arc cannon chain', Math.max(1, Math.floor((bolt.userData.damage || 1) / 2))); } } else if (bolt.position.distanceTo(robot.position) > 42) { scene.remove(bolt); bolts.splice(i, 1); } }
    if (!gateDone && robot.position.distanceTo(gate.position) < 1.55) { gateDone = true; score += 250; say('Calibration gate bonus collected. Continue with the shared mission objectives.'); } if (keyObject.visible && robot.position.distanceTo(keyObject.position) < 1) { hasKey = true; keyObject.visible = false; score += 250; playSound('pickup'); say('Key secured. Bring it to the locked door.'); } if (!doorOpen && level.door && robot.position.distanceTo(doorObject.position) < 2.1) { if (hasKey) { doorOpen = true; doorObject.visible = false; score += 300; playSound('pickup'); say('Key accepted. Door open.'); } else say('Door locked — find the glowing key first.'); } cells.forEach((c) => { if (c.visible && !c.userData.got && robot.position.distanceTo(c.position) < 1) { c.userData.got = true; c.visible = false; cellCount += 1; score += 150; playSound('pickup'); say(`Energy cell ${cellCount} of ${level.cells.length} secured.`); if (cellCount === level.cells.length) mark('cells', 'All cells secured. Dock after the room is safe.', 300); } }); if (cellCount === level.cells.length && enemies.every((e) => !e.userData.alive) && doorOpen && robot.position.distanceTo(dock.position) < 1.15) { const timeBonus = Math.max(0, level.bonus - Math.floor(levelElapsed) * 10), completedLevel = levelIndex + 1, earnedProgress = completedLevel > highestCompletedLevel, reward = earnedProgress ? unlockReward(completedLevel) : undefined; highestCompletedLevel = Math.max(highestCompletedLevel, completedLevel); saveProgress('robHighestCompletedLevel', highestCompletedLevel); updateWorkshop(); mark('dock', [reward, `Level cleared! Time bonus: ${timeBonus}.`].filter(Boolean).join(' '), 500 + timeBonus); playSound('level-complete'); running = false; levelComplete = true; ui.start.hidden = false; if (levelIndex < levels.length - 1) ui.start.textContent = `Continue to level ${levelIndex + 2}`; else { complete = true; ui.start.textContent = 'Play campaign again'; root.dispatchEvent(new CustomEvent('rob:campaign-complete', { detail: { score, durationSeconds: Math.round(elapsed), levelsCompleted: levels.length } })); say([reward, `Campaign complete with ${score.toLocaleString()} points. Add your call sign!`].filter(Boolean).join(' ')); } } };
  const fullscreenElement = () => document.fullscreenElement || document.webkitFullscreenElement;
  const isFullscreen = () => fullscreenElement() === shell || shell.classList.contains('is-pseudo-fullscreen');
  const viewportHeight = () => Math.round(window.visualViewport?.height || innerHeight);
  const resize = () => { const w = viewport.clientWidth, h = isFullscreen() ? viewportHeight() : Math.max(isTouch ? 460 : 420, Math.min(720, w * .58)); renderer.setSize(w, h, false); camera.aspect = w / h; camera.updateProjectionMatrix(); };
  const animate = () => { requestAnimationFrame(animate); const dt = Math.min(clock.getDelta(), .05); tick(dt); updateRobotWeapons(); cells.forEach((c, i) => { if (c.visible) { c.rotation.y += dt * 1.4; c.position.y = .55 + Math.sin(elapsed * 2 + i) * .08; } }); camera.position.lerp(new THREE.Vector3(robot.position.x + 6.2, 11.2, robot.position.z + 8.2), .06); camera.lookAt(robot.position.x, .65, robot.position.z - 1.8); ui.time.textContent = new Date(elapsed * 1000).toISOString().slice(14, 22); ui.score.textContent = score; ui.left.textContent = controls.left.toFixed(2); ui.right.textContent = controls.right.toFixed(2); ui.enemies.textContent = enemies.filter((e) => e.userData.alive).length; ui.health.value = health; ui.healthText.textContent = `${health} / ${MAX_ROB_HEALTH}`; const boss = currentBoss(); ui.boss.hidden = !boss; if (boss) { ui.bossName.textContent = `${boss.userData.name} shields`; ui.bossText.textContent = `${boss.userData.health} / ${boss.userData.maxHealth}`; ui.bossHealth.max = boss.userData.maxHealth; ui.bossHealth.value = Math.max(0, boss.userData.health); } renderer.render(scene, camera); };

  const updateTouchDrive = () => { touch.forward = THREE.MathUtils.clamp([...activeDrivePointers.values()].reduce((sum, input) => sum + input.forward, 0), -1, 1); touch.steering = THREE.MathUtils.clamp([...activeDrivePointers.values()].reduce((sum, input) => sum + input.steering, 0), -1, 1); };
  const releaseDrive = (pointerId) => { const input = activeDrivePointers.get(pointerId); if (!input) return; input.button.classList.remove('is-pressed'); input.button.setAttribute('aria-pressed', 'false'); activeDrivePointers.delete(pointerId); updateTouchDrive(); };
  const resetTreadStick = (stick, side) => { touch[side] = 0; touch[`${side}Active`] = false; stick.style.setProperty('--stick-x', '0px'); stick.style.setProperty('--stick-y', '0px'); stick.setAttribute('aria-valuenow', '0'); stick.setAttribute('aria-valuetext', 'Stopped'); stick.classList.remove('is-active'); };
  const updateTreadStick = (stick, side, clientX, clientY) => { const rect = stick.getBoundingClientRect(), x = clientX - rect.left - rect.width / 2, y = clientY - rect.top - rect.height / 2, visualLimit = Math.min(rect.width, rect.height) * .31, distance = Math.hypot(x, y), visualScale = distance > visualLimit ? visualLimit / distance : 1, rawValue = THREE.MathUtils.clamp(-y / (rect.height * .34), -1, 1), value = Math.abs(rawValue) < .06 ? 0 : rawValue; touch[side] = value; touch[`${side}Active`] = true; stick.style.setProperty('--stick-x', `${x * visualScale}px`); stick.style.setProperty('--stick-y', `${y * visualScale}px`); stick.setAttribute('aria-valuenow', value.toFixed(2)); stick.setAttribute('aria-valuetext', value === 0 ? 'Stopped' : `${Math.round(Math.abs(value) * 100)}% ${value > 0 ? 'forward' : 'reverse'}`); stick.classList.add('is-active'); };
  const releaseTread = (pointerId) => { const input = activeTreadPointers.get(pointerId); if (!input) return; activeTreadPointers.delete(pointerId); resetTreadStick(input.stick, input.side); };
  const releaseAllInput = () => { keys.clear(); [...activeDrivePointers.keys()].forEach(releaseDrive); [...activeTreadPointers.keys()].forEach(releaseTread); root.querySelectorAll('[data-tread-stick]').forEach((stick) => resetTreadStick(stick, stick.dataset.treadStick)); touch.forward = touch.steering = controls.left = controls.right = 0; gamepadLaserHeld = false; cancelLaserCharge(); };
  addEventListener('keydown', (e) => { if (['ArrowUp', 'ArrowDown', 'ArrowLeft', 'ArrowRight', 'KeyW', 'KeyA', 'KeyS', 'KeyD', 'Space', 'KeyQ'].includes(e.code)) e.preventDefault(); const firstPress = !keys.has(e.code); keys.add(e.code); if (firstPress && e.code === 'Space') saberSlash(); if (firstPress && e.code === 'KeyQ') beginLaserCharge(); }); addEventListener('keyup', (e) => { keys.delete(e.code); if (e.code === 'KeyQ') releaseLaserCharge(); }); addEventListener('blur', releaseAllInput);
  root.querySelectorAll('[data-drive]').forEach((button) => { const forward = Number(button.dataset.forward || 0), steering = Number(button.dataset.steering || 0); button.setAttribute('aria-pressed', 'false'); button.addEventListener('pointerdown', (e) => { e.preventDefault(); activeDrivePointers.set(e.pointerId, { forward, steering, button }); updateTouchDrive(); button.classList.add('is-pressed'); button.setAttribute('aria-pressed', 'true'); button.setPointerCapture?.(e.pointerId); }); ['pointerup', 'pointercancel', 'lostpointercapture'].forEach((eventName) => button.addEventListener(eventName, (e) => releaseDrive(e.pointerId))); });
  root.querySelectorAll('[data-tread-stick]').forEach((stick) => { const side = stick.dataset.treadStick; stick.addEventListener('pointerdown', (e) => { e.preventDefault(); if (touch[`${side}Active`]) return; activeTreadPointers.set(e.pointerId, { side, stick }); stick.setPointerCapture?.(e.pointerId); updateTreadStick(stick, side, e.clientX, e.clientY); }); stick.addEventListener('pointermove', (e) => { if (activeTreadPointers.get(e.pointerId)?.stick === stick) updateTreadStick(stick, side, e.clientX, e.clientY); }); ['pointerup', 'pointercancel', 'lostpointercapture'].forEach((eventName) => stick.addEventListener(eventName, (e) => { e.preventDefault(); releaseTread(e.pointerId); })); stick.addEventListener('keydown', (e) => { if (!['ArrowUp', 'ArrowDown'].includes(e.code)) return; e.preventDefault(); e.stopPropagation(); const value = e.code === 'ArrowUp' ? 1 : -1, rect = stick.getBoundingClientRect(); updateTreadStick(stick, side, rect.left + rect.width / 2, rect.top + rect.height * (.5 - value * .34)); }); stick.addEventListener('keyup', (e) => { if (!['ArrowUp', 'ArrowDown'].includes(e.code)) return; e.preventDefault(); e.stopPropagation(); resetTreadStick(stick, side); }); });
  ui.laserButtons.forEach((button) => { button.addEventListener('pointerdown', (e) => { e.preventDefault(); beginLaserCharge(); button.setPointerCapture?.(e.pointerId); }); button.addEventListener('pointerup', (e) => { e.preventDefault(); releaseLaserCharge(); }); button.addEventListener('pointercancel', (e) => { e.preventDefault(); cancelLaserCharge(); }); button.addEventListener('lostpointercapture', releaseLaserCharge); }); root.querySelectorAll('[data-sim-saber]').forEach((button) => button.addEventListener('pointerdown', (e) => { e.preventDefault(); saberSlash(); }));
  const enterPseudoFullscreen = () => { shell.classList.add('is-pseudo-fullscreen'); document.body.classList.add('rob-game-open'); };
  const leavePseudoFullscreen = () => { shell.classList.remove('is-pseudo-fullscreen'); document.body.classList.remove('rob-game-open'); };
  const syncFullscreen = () => { const open = Boolean(fullscreenElement()) || shell.classList.contains('is-pseudo-fullscreen'); shell.classList.toggle('is-fullscreen', open); ui.fullscreen.textContent = open ? '× Exit' : '⛶ Fullscreen'; ui.fullscreen.setAttribute('aria-label', open ? 'Exit fullscreen' : 'Enter fullscreen'); releaseAllInput(); requestAnimationFrame(resize); };
  ui.fullscreen.addEventListener('click', async () => { try { if (fullscreenElement()) await (document.exitFullscreen?.() || document.webkitExitFullscreen?.()); else if (shell.classList.contains('is-pseudo-fullscreen')) leavePseudoFullscreen(); else if (shell.requestFullscreen) await shell.requestFullscreen({ navigationUI: 'hide' }); else if (shell.webkitRequestFullscreen) shell.webkitRequestFullscreen(); else enterPseudoFullscreen(); if (isFullscreen()) screen.orientation?.lock?.('landscape')?.catch?.(() => {}); syncFullscreen(); } catch { enterPseudoFullscreen(); syncFullscreen(); } }); ['fullscreenchange', 'webkitfullscreenchange'].forEach((eventName) => document.addEventListener(eventName, syncFullscreen)); window.visualViewport?.addEventListener('resize', resize); addEventListener('orientationchange', () => requestAnimationFrame(resize));
  ui.start.addEventListener('click', () => { if (complete) reset(); else if (levelComplete) { levelIndex += 1; loadLevel(levelIndex); } running = true; startMusic(); playSound('mission-start'); ui.start.hidden = true; say(`Level ${levelIndex + 1}: clear the targets, collect every cell, solve the key and door when present, then dock.`); viewport.focus(); }); ui.reset.addEventListener('click', reset); root.querySelector('[data-sim-sound]').addEventListener('click', (event) => { soundEnabled = !soundEnabled; if (!soundEnabled) window.speechSynthesis?.cancel?.(); event.currentTarget.setAttribute('aria-pressed', String(soundEnabled)); event.currentTarget.textContent = soundEnabled ? '♪ Effects' : 'Effects off'; }); root.querySelector('[data-sim-music]').addEventListener('click', (event) => { musicEnabled = !musicEnabled; event.currentTarget.setAttribute('aria-pressed', String(musicEnabled)); event.currentTarget.textContent = musicEnabled ? '♫ Techno' : 'Music off'; if (musicEnabled && running) startMusic(); else stopMusic(); });
  ui.finish.addEventListener('change', () => { selectedFinishID = ui.finish.value; saveProgress('robRobotFinish', selectedFinishID); applyLoadout(); say(`${selectedFinish().name} finish equipped.`); });
  ui.ranged.addEventListener('change', () => { const requested = rangedWeapons.find(({ id }) => id === ui.ranged.value); if (!requested || !isUnlocked(requested, highestCompletedLevel)) { say(`Complete Level ${requested?.requiredLevel ?? 0} to unlock ${requested?.name ?? 'that weapon'}.`); updateWorkshop(); return; } selectedRangedID = requested.id; saveProgress('robRangedWeapon', selectedRangedID); applyLoadout(); say(`${requested.name} equipped.`); });
  ui.melee.addEventListener('change', () => { const requested = meleeWeapons.find(({ id }) => id === ui.melee.value); if (!requested || !isUnlocked(requested, highestCompletedLevel)) { say(`Complete Level ${requested?.requiredLevel ?? 0} to unlock ${requested?.name ?? 'that weapon'}.`); updateWorkshop(); return; } selectedMeleeID = requested.id; saveProgress('robMeleeWeapon', selectedMeleeID); applyLoadout(); say(`${requested.name} equipped.`); });
  document.addEventListener('visibilitychange', () => { if (document.hidden) releaseAllInput(); }); addEventListener('pagehide', releaseAllInput); new ResizeObserver(resize).observe(viewport); reset(); resize(); animate();
}
